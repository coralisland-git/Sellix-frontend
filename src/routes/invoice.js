import {
    Invoice,
  } from 'screens'
  
  const invoiceRoutes = [
    {
      path: '/invoice/:id',
      name: 'Bitcoin',
      component: Invoice.screen
    }
  ]
  
  export default invoiceRoutes